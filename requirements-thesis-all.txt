# Shared thesis_venv — BM1 + Pattern A + Pattern B (install once at repo root)
#   python3 -m venv thesis_venv
#   thesis_venv/bin/pip install -r requirements-thesis-all.txt
# Or: ./scripts/setup_thesis_venv.sh

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
scikit-learn>=1.3.0
tqdm>=4.65.0
PyYAML>=6.0
pyaxmlparser>=0.3.31
